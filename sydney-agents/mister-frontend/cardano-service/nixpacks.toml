# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci --production=false"]

[phases.build]
cmds = ["echo 'No build required for Node.js service'"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"